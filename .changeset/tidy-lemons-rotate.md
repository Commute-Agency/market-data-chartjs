---
"@commute/market-data-chartjs": minor
---

Add optional `data-gradient-angle` attribute on the canvas to rotate the chart's fill gradient clockwise by a given number of degrees, instead of always being strictly vertical. Defaults to `0`, so existing charts render unchanged.
